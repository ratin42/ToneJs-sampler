-   Delete the track array and replace it by the trackMapping

-   MultiMode:

    -   handle bank switching in MultiMode
    -   handle TuneDecayMix in MultiMode

-   Sequencing
    -   add Mute Solo in Setup Menu
